void subsets(int x)
{
     int range = 1LL << x;

     for (int mask = 0; mask < range; mask++)
     {
          for (int i = 0; i < x; i++)
          {
               if (mask & (1LL << i))
                    
          }
     }

}
